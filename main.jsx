import React from "react";
import ReactDOM from "react-dom/client";
import BorrowingCapacityApp from "./BorrowingCapacityApp";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <BorrowingCapacityApp />
  </React.StrictMode>
);